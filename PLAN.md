# 공백기 며칠째

아이돌이 마지막 컴백 이후 며칠째인지, 평균 주기를 넘겼는지 보여주는 사이트.

- **사이트** https://gongbaekgi.vercel.app
- **저장소** https://github.com/J-DONGHYUN/gongbaekgi (public)
- **설계서** https://claude.ai/code/artifact/23aa8f23-e1dd-46fc-9324-bfe83445c5bd
- **팀 공유용 설명** https://claude.ai/code/artifact/a5035111-e12f-4be9-8d0b-8edc494b6f17

현재 **38팀 · 컴백 490건**. 배포·자동갱신·투표 전부 동작 중. 홍보는 아직 시작 안 함.

---

## 명령어

```bash
npm run dev         # 로컬 미리보기
npm run build       # 빌드 (OG 이미지 자동 재생성)

npm run fetch       # iTunes 에서 발매 이력 수집 → data/artists.json
npm run fetch:test  # 3팀만 콘솔 출력, 저장 안 함
npm run og          # OG 이미지만 재생성 → public/og/
npm run ids         # 아티스트 ID 재확인 (미리보기)
npm run ids:apply   # 아티스트 ID 기록
```

배포는 `git push` 만 하면 된다. 무중단이고 빌드가 실패하면 교체되지 않는다.

---

## 무엇이 자동이고 무엇이 수동인가

| | 방식 |
|---|---|
| 공백기 일수 | **자동** — 브라우저에서 오늘 날짜로 계산. 매일 늘어남. 배포 불필요 |
| 하트·순위 | **자동** — Redis 에 실시간 반영, 캐시 없음 |
| 새 컴백 반영 | **자동** — GitHub Actions 가 하루 두 번 |
| 팀 추가·제외 | **수동** — `scripts/artists.config.json` 수정 후 `npm run fetch && npm run og` |

### 자동 갱신 (`.github/workflows/update-data.yml`)

21시·05시 KST 실행. 수동 실행(workflow_dispatch)도 가능.
수집 → 검증 → OG 재생성 → 변경 있으면 커밋 → Vercel 자동 배포.

안전장치 두 개:
- 직전 커밋 대비 팀·컴백이 **10% 이상 급감하면 커밋하지 않고 실패**시킨다
- `fetchedAt` 만 바뀐 경우 파일을 원상복구해 무의미한 커밋을 막는다

---

## 확정된 결정

| 항목 | 결정 |
|---|---|
| 컴백 정의 | 정규·미니만 (리패키지 포함, 싱글·OST·해외반 제외) |
| 데이터 소스 | **iTunes Search API** (키 불필요) |
| 정확도 정책 | 자동 집계 그대로 + 오차 명시. 수동 검수 안 함 |
| 메인 화면 | 공백기 카운터. 공백기 긴 순 랭킹은 만들지 않음 |
| 호스팅 | Vercel Hobby (무료, 비상업 용도) |
| 투표 저장소 | Upstash Redis (무료, 서울 리전) |
| 분석 | GA4 `G-Y3N2BGPL4J` |

### 제외한 팀 (`disabled: true`)

`scripts/artists.config.json` 에서 `disabled` 줄만 지우면 되돌아온다. ID 는 남겨뒀다.

- **NewJeans** — 공백기가 법적 분쟁 때문이라 숫자를 보여주는 것 자체가 편들기로 읽힐 수 있음.
  되돌리면 `뉴진스 공백기` 검색 유입을 얻지만 그 위험도 함께 온다
- **JENNIE · JISOO · ROSÉ · LISA** — 컴백 3회 미만이라 주기를 낼 수 없음

---

## 계산 규칙

```
컴백 판정
  include_groups="album" · 4곡 이상
  이름 제외: single / remix / inst / live / ost / soundtrack /
            japanese / tokyo / chinese / the best / skz-replay
  30일 안 연속 발매는 1회로 병합
  미래 날짜(예약 발매) 제외

계산
  기준주기 = median(최근 5회 간격)      // 평균 아님, 이상치 방어
  공백기   = 오늘 - 마지막컴백일          // 브라우저에서 계산
  비율     = 공백기 / 기준주기
    < 0.7      활동 주기 안
    0.7 ~ 1.0  슬슬 나올 때
    1.0 ~ 1.5  평균보다 N일 더 기다리는 중
    >= 1.5     평균 컴백 주기의 N배
  예상범위 = 마지막컴백일 + 기준주기 ± 30% (최소 ±20일)
  컴백 3회 미만 → 데이터 부족, 예측 숨김

기다림 순위
  기다림 = 하트 수 × (1 + 공백기 / 365)
  최근 이틀치만 집계 · 팀당 IP 기준 60초 쿨다운 · 하트 수는 비공개
```

---

## 알려진 한계

- 제목에 `Japanese` 가 없는 **일본 발매반이 걸러지지 않는다** (IVE `WAVE`·`Be Alright`).
  공백기 자체는 맞지만 평균 주기가 실제보다 짧아진다
- **투표 조작을 완전히 막을 수 없다.** IP 쿨다운과 이틀 윈도우로 이득을 줄였을 뿐.
  유입이 생기면 Cloudflare Turnstile(무료·보이지 않는 캡차) 추가를 검토할 것
- 43팀 전수 검증은 **마지막 컴백만** 했다. 전체 이력은 미검증

---

## 다시 헤매지 말 것

### 데이터 소스
- **Spotify 불가** — 2026년부터 앱 소유자에게 Premium 구독을 요구, 전 엔드포인트 403
- **Deezer 불가** — K-pop 카탈로그가 비어 있다 (TWICE·IU 가 정규/미니 0장)
- **벅스 불가** — robots.txt 가 화이트리스트. 검색엔진 봇만 허용

### iTunes
- 아티스트 검색은 K-pop 에 부정확 → **앨범 검색 + `attribute=artistTerm`** 으로 artistId 역추적
- 한국 스토어는 팀명이 한글인 경우가 많다 (BTS→방탄소년단) → 영문·한글 둘 다로 검색
- `(G)I-DLE` 은 `i-dle` 로 개명, `LISA` 는 등록명이 `리사 (LISA)`, `&TEAM` 은 J-Pop 태그가 정상

### 문자열 처리
- `normalize("NFD")` 는 **한글을 자모로 분해**한다. 악센트만 지우고 **NFC 로 되돌린 뒤** 필터링할 것.
  이걸 놓쳐서 26팀이 매칭 실패했고, 슬러그에서는 `ROSÉ → /ros/` 가 됐다

### OG 이미지
- Next 의 `opengraph-image.js` 규칙은 정적 내보내기와 궁합이 나쁘다 (확장자 없는 파일 → octet-stream).
  `scripts/og.mjs` 가 `next/og.js` 를 직접 불러 **진짜 .png** 로 떨어뜨린다
- satori 는 **woff2 를 못 읽는다.** Google Fonts 는 UA 로 포맷을 정한다 —
  IE 계열 → EOT(못 씀), **구형 안드로이드 → TTF(정답)**
- satori 는 자식이 2개 이상인 div 에 `display:flex` 를 요구한다.
  `아이돌 {n}팀` 처럼 텍스트+표현식이 섞이면 자식 3개로 세어 에러난다

### Next.js
- `output: export` 를 뺀 뒤 `next build` 는 **`out/` 에 쓰지 않는다.** 확인은 배포된 사이트로
- `generateStaticParams` 에 없는 주소는 기본적으로 즉석 렌더된다.
  `dynamicParams = false` 가 없으면 제외한 팀이 빈 화면 200 으로 살아 있다
- 루트 레벨 `sitemap.js`·`robots.js` 는 정적 내보내기에서 `dynamic = "force-static"` 필요

---

## 절대 하지 말 것

- 브라우저에서 외부 API 를 직접 호출 (키 노출)
- 서버를 더 늘리기 — 함수는 `/api/vote`, `/api/ranking` 둘뿐. 로그인·세션·유저 테이블 금지
- **공백기 긴 순 랭킹** — 특정 그룹 조리돌림으로 읽힌다.
  지금 홈의 순위대는 팬이 직접 누른 하트 기반이라 성격이 다르다
- 투표 수 공개 — 조작 성과가 보이면 조작 동기가 생긴다
- 아티스트 사진 — 제공 API 도 사용 허락도 없다. 캡처해 올리는 건 더 위험
- 앨범 커버를 OG 이미지에 합성 — iTunes 약관 조건(스토어 링크 옆)을 벗어난다
- 정규화 규칙 무한 확장 — 안 맞는 팀은 목록에서 빼는 게 빠르다

### 문구 톤
공백기를 조롱이 아니라 기다림으로. **"방치 중" 금지, "기다리는 중" 사용.**

---

## 홍보

실행 문서는 [PROMO.md](PROMO.md) — 2일 일정, 계정 세팅, 팀별 트윗 문구, DM 문구, 금지 사항.

핵심 전제만 여기 남긴다.

- 팔로워 0 계정이 링크를 던지면 무시되거나 도달이 눌린다.
  **본문에는 카드 이미지, 링크는 답글**로 우회한다.
- 사이트가 아니라 **특정 팀의 숫자**를 게시한다. OG 카드가 이미 큰 이미지로 나간다.
- 가장 강한 계기는 **컴백 발표** ("공백기 N일 만에 컴백"). 매주 있으니 기다릴 필요 없다.
- 첫 주 성공 기준은 방문자 수가 아니라 **팬이 우리 카드를 캡처해 올렸는가**.

## 판단 대기 중

### 영어 번역 — 아직 아님
사용자 0명 상태에서 번역하면 표면적만 두 배(4~6시간). 계산 방식 페이지는 기계번역이 안 통한다.

**판단 기준**: GA4 → 잠재고객 → 국가/언어에서 **비한국 유입이 20~30% 넘으면** 그때.
해외 K-pop 팬덤은 X 에서 더 활발해서 공유가 돌기 시작하면 빠르게 뒤집힐 수 있다.
할 때도 전체가 아니라 **카운터 화면과 OG 이미지만** 먼저.

### 그 밖
- 커스텀 도메인 — 사면 `lib/site.js` 한 줄 + `npm run og` 재생성
- 서치콘솔 · 네이버 서치어드바이저 등록 — 인증 코드 자리는 `lib/site.js` 에 준비됨.
  **네이버는 등록 안 하면 색인이 거의 안 된다**
- 제보 링크 (구글폼) — 데이터 검증을 사용자에게 위임. 10분
- 상위 10팀 이력 정밀 검증 — GA4 로 인기 팀 확인 후

---

## GA4 에서 볼 것

| 지표 | 경로 | 왜 |
|---|---|---|
| **재방문율** | 참여도 → 개요 | **제품 성패.** Vercel Analytics 로는 원리적으로 못 본다 |
| 인기 페이지 | 참여도 → 페이지 및 화면 | 어느 팬덤이 반응하는지 → 커버 확대 우선순위 |
| 유입 경로 | 획득 → 트래픽 획득 | 초반엔 트위터여야 정상 |
| 공유 클릭 | 참여도 → 이벤트 → `click` | 공유가 실제로 일어나는지 |

검색 유입은 3~6개월 뒤. **첫 주에 0인 건 정상이다.**
