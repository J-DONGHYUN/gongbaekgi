# 절대 커밋 금지 — Spotify Client Secret
.env
.env.local
.env*.local

node_modules/
.next/
out/
.vercel/
.DS_Store
*.log
/.next/
/out/

# 홍보용 카드 — 커밋하지 않는다.
# 39장 3MB 라서 매일 갱신하면 저장소가 불어나고, 숫자가 묵으면 안 되니
# 올리기 직전에 npm run promo 로 새로 만든다.
public/promo/
