# 공백기 며칠째

아이돌의 마지막 컴백 이후 경과일과 평균 컴백 주기를 보여주는 정적 사이트.
Spotify Web API에서 발매 이력을 받아 계산만 한다. 실행 계획은 [PLAN.md](PLAN.md).

사용자는 바이브코딩으로 작업하며 **코드를 직접 읽지 않는다.**
따라서 아래 제약은 사용자가 검토로 잡아낼 수 없으니 반드시 지킬 것.

## 절대 규칙

1. **Spotify 호출은 로컬 스크립트에서만.** 브라우저/클라이언트 코드에서 호출 금지 — Client Secret이 노출된다.
2. **`.env`는 절대 커밋하지 않는다.** `.gitignore` 확인 후 작업.
3. **서버를 만들지 않는다.** DB, API 라우트, 로그인, 세션 전부 금지. 모든 페이지는 SSG, 상태가 필요하면 `localStorage`.
4. **공백기 일수는 빌드가 아니라 브라우저에서 오늘 날짜로 계산한다.** 재배포 없이 매일 올라가야 한다.
5. **JSON에는 사실만 저장한다.** 공백기·주기·상태·예측은 저장하지 말고 화면에서 계산.

"더 편하게 하려면 DB를 붙이자" 류의 제안은 하지 말 것. 이 전제(서버 0 · 무료 · 2일)가 프로젝트의 정의다.

## 도메인 규칙

컴백 = **정규·미니 앨범 발매만.** 판정·계산 규칙 전문은 PLAN.md에 있고, 코드를 고칠 때 그쪽을 기준으로 삼는다.

문구 톤: 공백기를 조롱이 아니라 기다림으로. "방치 중" 금지, "기다리는 중" 사용.
랭킹·팀 간 비교 화면은 만들지 않는다 (팬덤 정서 리스크).

## 구조

```
scripts/     로컬 수집 스크립트 (Spotify 호출은 여기서만)
data/        수집 결과 JSON — 앱은 이것만 읽는다
app/         Next.js App Router, 전부 정적 생성
.env         Client ID/Secret — 커밋 금지
```

## 작업 원칙

- 정확도가 기능보다 우선. 데이터가 틀리면 이 제품은 가치가 0이다.
- 안 맞는 아티스트는 규칙을 무한히 늘리지 말고 목록에서 뺀다.
- 2일 안에 배포가 목표. 스코프를 넓히자는 제안보다 줄이자는 제안을 할 것.
