# 공백기 며칠째

아이돌의 마지막 컴백 이후 경과일과 평균 컴백 주기를 보여주는 정적 사이트.
Spotify Web API에서 발매 이력을 받아 계산만 한다. 실행 계획은 [PLAN.md](PLAN.md).

사용자는 바이브코딩으로 작업하며 **코드를 직접 읽지 않는다.**
따라서 아래 제약은 사용자가 검토로 잡아낼 수 없으니 반드시 지킬 것.

## 절대 규칙

1. **Spotify 호출은 로컬 스크립트에서만.** 브라우저/클라이언트 코드에서 호출 금지 — Client Secret이 노출된다.
2. **`.env`는 절대 커밋하지 않는다.** `.gitignore` 확인 후 작업.
3. **서버는 하트 투표에만.** `app/api/vote`, `app/api/ranking` 두 개가 전부다.
   그 밖에는 만들지 않는다 — 로그인·세션·유저 테이블 금지. 모든 페이지는 여전히 SSG 로 정적 생성된다.
   투표 쿨다운은 반드시 **서버에서 IP 기준**으로 판정한다. 브라우저 기준은 개발자도구로 뚫린다.
4. **공백기 일수는 빌드가 아니라 브라우저에서 오늘 날짜로 계산한다.** 재배포 없이 매일 올라가야 한다.
5. **JSON에는 사실만 저장한다.** 공백기·주기·상태·예측은 저장하지 말고 화면에서 계산.

서버를 더 늘리자는 제안은 하지 말 것. **무료 한도 안에서 돌아가는 게 전제**다.
Hobby 플랜은 함수 100만 호출/월이고, 순위 조회는 60초 캐시로 호출을 억제하고 있다.
새 기능이 함수 호출을 늘리면 그 캐시 전략을 먼저 확인할 것.

## 도메인 규칙

컴백 = **정규·미니 앨범 발매만.** 판정·계산 규칙 전문은 PLAN.md에 있고, 코드를 고칠 때 그쪽을 기준으로 삼는다.

문구 톤: 공백기를 조롱이 아니라 기다림으로. "방치 중" 금지, "기다리는 중" 사용.

**순위는 "기다림 지수"로만 만든다.** 공백기 긴 순 랭킹은 특정 그룹을 조리돌리는 모양이 되므로
만들지 않는다. 지금 홈의 순위대는 성격이 다르다 — 팬이 직접 누른 하트에 공백기를 곱한 것이고,
"기다린다"는 응원 프레임이다. 이 구분을 지킬 것.

투표수 자체는 공개하지 않는다. 조작 성과가 눈에 보이면 조작 동기가 생긴다.
K-pop 팬덤은 조직적 투표 문화가 있어서 이 부분이 특히 민감하다.

## 구조

```
scripts/     로컬 수집 스크립트 + OG 이미지 생성
data/        수집 결과 JSON — 화면은 이것만 읽는다
public/og/   생성된 OG 이미지 (npm run og)
lib/         calc(계산) · votes(투표 저장소) · data · site(도메인·인증코드)
app/         Next.js App Router. 페이지는 전부 SSG
app/api/     하트 투표·순위 두 개뿐
```

## 작업 원칙

- 정확도가 기능보다 우선. 데이터가 틀리면 이 제품은 가치가 0이다.
- 안 맞는 아티스트는 규칙을 무한히 늘리지 말고 목록에서 뺀다.
- 2일 안에 배포가 목표. 스코프를 넓히자는 제안보다 줄이자는 제안을 할 것.
